/**
 * Window title-bar height — shared by the main window's tab bar (AppShellTabBar) and the
 * detached sub-window's title bar (ConversationShell, in detached mode). Sized so its
 * vertically-centered content lines up with the macOS traffic lights at their inset
 * (windowRegistry `trafficLightPosition.y = 13` for both Main and SubWindow).
 */
export const TITLE_BAR_HEIGHT_PX = '37.5px'
export const TITLE_BAR_HEIGHT_CLASS = 'h-[37.5px]'
